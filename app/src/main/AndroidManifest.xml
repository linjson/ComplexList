<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="example">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="example.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="example.list.SwipeDragActivity"/>
        <activity android:name="example.refreshpull.BaseScrollTest"/>
        <activity android:name="example.refreshpull.RefreshListTest"/>
        <activity android:name="example.refreshpull.BaseNestScrollTest"/>
        <activity android:name="example.emptyview.EmptyViewTest"/>
        <activity android:name="example.fixheader.FixHeaderActivity"/>
        <activity android:name="example.refreshpull.CoorRefreshListTest"/>


    </application>

</manifest>